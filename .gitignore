# Dependencies
node_modules/

# Builds
dist/

# Environment — never commit secrets
.env
.env.*
!.env.example

# Platform
.vercel/

# Python virtualenvs (dev only — the worker image installs its own)
.venv/
venv/

# OS / logs
.DS_Store
npm-debug.log*
*.log
__pycache__/
*.pyc

# Build-time SSR and audit artifacts
.prerender/
.seo-audit/
